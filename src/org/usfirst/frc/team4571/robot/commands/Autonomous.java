package org.usfirst.frc.team4571.robot.commands;

import edu.wpi.first.wpilibj.command.CommandGroup;

/**
 * Blank Autonomous
 */
public class Autonomous extends CommandGroup {
    
    public  Autonomous() {
        // Add Commands here:
        // e.g. addSequential(new Command1());
    		  //addSequential(new Grabber(0, true));
    		  //addSequential(new Elevator(2, true, false));
              //addSequential(new Driving(true, true,100));  
             // addSequential(new Grabber(1, true));
              //addSequential(new Elevator(1, true, false));
              //addSequential(new Driving(true, true,120));
              //addSequential(new Grabber(0, true));
              //addSequential(new Elevator(2, true, false));
              //addSequential(new Driving(true, true,150));
              //addSequential(new Grabber(1, true));
              //addSequential(new Elevator(1, true, false));
            //addSequential(new Elevator(1, true, false));
    	//Trashcan elevator FUCKING 70!!!!!!!! POSITIVE DRIVING
    			/*addSequential(new Grabber(1, true));
         
              addSequential(new Elevator(1, true, false));
              addSequential(new Driving(true, true,75));*/
              //Yellow ELEVATOR 50 IDIOTS!!!!!!! arms should b all the way down
              //addSequential(new Grabber(1, true));
              //addSequential(new Elevator(1, true, false));
              //addSequential(new Driving(true, false,700));
             // addSequential(new Driving(true, true,120));
              //addSequential(new Driving(true, false,450));
              //addSequential(new Driving(true, false,250));
              //addSequential(new Elevator( 2, true, false));
             // addSequential(new Grabber(0, true));
              
              
        // these will run in order.

        // To run multiple commands at the same time,
        // use addParallel()
        // e.g. addParallel(new Command1());
        //      addSequential(new Command2());
        // Command1 and Command2 will run in parallel.

        // A command group will require all of the subsystems that each member
        // would require.
        // e.g. if Command1 requires chassis, and Command2 requires arm,
        // a CommandGroup containing them would require both the chassis and the
        // arm.
    }
}
